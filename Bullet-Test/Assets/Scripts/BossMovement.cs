using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//hola soy una modificacion
public class BossMovement : MonoBehaviour {

    public Boss boss;
    public float time;
    private bool pupo = false; //hola
    
    void Start () {
       
	}
	
	
	void Update () {
       //time += Time.deltaTime;
       //int a = (int)Math.Round(time);
        //la condicion esta horrible pero es un comienzo
        //cuando se mueve no dispara. Eso se puede cambiar.
        //Ver WaypointsMovement() en clase Boss
        
	}
}
